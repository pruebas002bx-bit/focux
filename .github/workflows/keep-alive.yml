name: Wake Up Focux Service

on:
  schedule:
    # Se ejecuta cada 14 minutos
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the service
        run: curl -s "https://focux-app.onrender.com/health"